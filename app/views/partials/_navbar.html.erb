<nav class="nav has-shadow">
    <div class="container">
        <div class="nav-left nav-menu">
            <a href="/" class="nav-item">
                <img src="http://i.imgur.com/KnWaDeC.png?1" width="70">
            </a>
            <% if user_signed_in? %>
            <%= link_to "My Notes", notes_path, class: "nav-item is-tab is-active" %>
            <%= link_to "New Note", new_note_path, class:"nav-item is-tab" %>
            <% end %>
        </div>
        <div class="nav-right nav-menu is-hidden-mobile">
            <% if user_signed_in? %>
                <%= link_to current_user.name, edit_user_registration_path, class:"nav-item is-tab is-hidden-mobile" %>
                <%= link_to "Log Out", destroy_user_session_path, method: :delete, class:"nav-item is-tab is-hidden-mobile" %>
            <% else %>
                <%= link_to "Log In", new_user_session_path, class:"nav-item is-tab" %>
                <%= link_to "Sign Up", new_user_registration_path, class:"nav-item is-tab" %>
            <% end %>
            </div>
        </div>
    </nav>
